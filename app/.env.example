OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o

# PostgreSQL Database URL (optional - if not set, uses in-memory storage)
# Single database for both student and faculty data
# DATABASE_URL=postgresql://advi_user:advi_password@localhost:5432/advi_db
